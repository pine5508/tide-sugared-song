
    <div class="jumbotron">
        <h1>Rohan's Bio</h1>
        <p>About me!</p>
    </div>
<hr>

<div class="col bg-success text-light">
  <h4>
    I am a rising sophomore in Massachusetts taking the course in summer 2021. 
    I am know english and marathi, and am learning spanish, which makes me trilingual.
  </h4>
</div>
<div class="col bg-dark text-light">
  <h1>
    Favorites:
  </h1>
  <ul>
    <h3>
      <li>Color: Orange</li>
      <li>Food: Potato Fries</li>
      <li>Number: 5</li>
      <li>Dessert: Brownies</li>
      <li>Sport: Badminton</li>
      <li>Subject: Math</li>
    </h3>
</ul>
</div>

email: rohankelkar@brandeis.edu

<hr>
<h2>
  Leave a comment on this page!
</h2>

<hr>
<% if (!loggedIn) {%>

   <b> You must be logged in to leave a comment! </b>

<%} else {%> 

  <hr>
<div>
  <div class="row">
    <div class="col">
      <h2>Leave a Comment</h2>
      <form method="post" action="/addCommentForRohan">
        <div class="row mb-3">
          <div class="col">
            <input type="text" class="form-control" name="title" placeholder="Title">
          </div>
          <div class="col">
            <input type="text" class="form-control" name="rating" placeholder="Rating (1-10)">
          </div>
        </div>
        <div class="mb-3">
          <textarea class="form-control" name="text" rows="2" placeholder="Type something..."></textarea>
        </div>
        <button type="submit" class="btn btn-primary mb-3">Submit</button>
      </form>
    </div>
    <div class="col">
      <h2>Comments</h2>
      <ul>
        <% comments.forEach(comment => { %>
        <li>
          <%= comment.title %> <i><%= comment.createdAt %></i>
          <br>
          <%= comment.text %>
          <br>
          Rating: <%= comment.rating %>
        </li>
        <% }) %>
      </ul>
    <% if (loggedIn) {%>
      <% if (['rohankelkar@brandeis.edu','tjhickey@brandeis.edu', 'joungwon04@brandeis.edu'].includes(user.googleemail)) { %>
      <div style="color:red">
          <pre><%= JSON.stringify(comments,null,5) %></pre>
      </div>
    </div>
  </div>
</div>

<%}%>



</div>

<% if (loggedIn) {%>
  <% if (['rohankelkar@brandeis.edu','tjhickey@brandeis.edu', 'joungwon04@brandeis.edu'].includes(user.googleemail)) { %>
  <div style="color:red">

    Here are the comments in raw JSON form:
    <pre>
      <%= JSON.stringify(comments,null,5) %>
    </pre>
    <!-- the <pre> tag preserves the spacing and JSON.stringify makes it look pretty -->
  <% } %>
<% } %>